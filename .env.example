# Example environment variables for local development
NEXTAUTH_URL=http://localhost:3000
# Generate a strong secret (e.g. `openssl rand -hex 32`)
NEXTAUTH_SECRET=replace_with_a_strong_secret

# Optional: protect initial admin bootstrap endpoint
# Generate a secret and set ADMIN_SETUP_SECRET to allow create-initial-admin
ADMIN_SETUP_SECRET=replace_with_admin_setup_secret

# Postgres (optional - for production)
# DATABASE_URL=postgres://user:password@host:5432/dbname
# PGSSLMODE=require

# Local SQLite override removed; project is Postgres-only
