# API Configuration
# For development, the app automatically uses window.location.origin/api
# This variable is mainly for SSR (server-side rendering) contexts
NEXT_PUBLIC_API_BASE_URL=/api

# Languages
NEXT_PUBLIC_SUPPORTED_LANGUAGES=ko,en,vi
NEXT_PUBLIC_DEFAULT_LANGUAGE=ko

# Vercel Deployment
# VERCEL_ENV=production
# VERCEL_URL=wms-wireframe.vercel.app
