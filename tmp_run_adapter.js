const { getAdapter } = require('./src/lib/adapters'); const { getShop } = require('./src/lib/shops'); (async()=>{ try{ const shopId='onedns2'; const shop=await getShop(shopId); console.log('shop credentials:', shop?shop.credentials:null); const adapter=getAdapter(shop?.platform); console.log('adapter loaded:', !!adapter); if(adapter){ const r=await adapter(shopId,{}); console.log('result length:', Array.isArray(r)?r.length:typeof r); console.log(JSON.stringify((Array.isArray(r)?r.slice(0,3):r),null,2)); } }catch(e){ console.error('error',e); } })();